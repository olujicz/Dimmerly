<svg width="1024" height="1024" viewBox="0 0 1024 1024" fill="none" xmlns="http://www.w3.org/2000/svg">
  <!-- Dimmerly app icon - GLOW layer.
       Painted on a full-canvas rect, not a circle: Icon Composer applies its
       edge highlight to a layer's vector path, so a <circle> here renders as a
       visible hard ring through the ray tips. The rect has no such contour. -->
  <defs>
    <radialGradient id="glow" cx="0" cy="0" r="1" gradientUnits="userSpaceOnUse" gradientTransform="translate(512 512) rotate(90) scale(300)">
      <stop stop-color="#9DE0FF" stop-opacity="0.55"/>
      <stop offset="1" stop-color="#9DE0FF" stop-opacity="0"/>
    </radialGradient>
  </defs>
  <rect width="1024" height="1024" fill="url(#glow)"/>
</svg>
